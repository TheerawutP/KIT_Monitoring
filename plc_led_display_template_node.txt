<style>
html, body {
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #111;
    font-family: sans-serif;
}

.wrapper {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: 10px;              /* ระยะรวมระหว่างบล็อก */
    padding: 6px 10px;
    box-sizing: border-box;
}

.section {
    display: flex;
    flex-direction: column;
}

.section-title {
    text-align: left;
    font-weight: bold;
    font-size: 15px;
    margin-bottom: 8px;     /* เว้นจากไฟด้านล่าง */
    color: #000;
}

.section-y {
    margin-top: 6px;        /* เว้นจาก INPUT X ด้านบน */
}

.grid {
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    gap: 5px;
    justify-items: center;
}

.cell {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.lamp {
    width: min(3.4vw, 26px);
    height: min(3.4vw, 26px);
    border-radius: 50%;
    background: #0a0a0a;
    border: none;
}

.lamp.on {
    background: #00ff3b;
    box-shadow: 0 0 6px rgba(0, 255, 59, 0.45);  /* glow เบา ๆ */
}

.label {
    font-size: 9px;
    margin-top: 1px;
    color: #777;
}
</style>

<div class="wrapper" ng-if="msg.payload">

    <div class="section section-x">
        <div class="section-title">INPUT X</div>
        <div class="grid">
            <div class="cell" ng-repeat="bit in msg.payload.X track by $index">
                <div class="lamp" ng-class="{'on': bit}"></div>
                <div class="label">X{{ ($index).toString(8) }}</div>
            </div>
        </div>
    </div>

    <div class="section section-y">
        <div class="section-title">OUTPUT Y</div>
        <div class="grid">
            <div class="cell" ng-repeat="bit in msg.payload.Y track by $index">
                <div class="lamp" ng-class="{'on': bit}"></div>
                <div class="label">Y{{ ($index).toString(8) }}</div>
            </div>
        </div>
    </div>

</div>
