<style>
    .plc-container {
        background-color: #1a1a1a;
        padding: 10px;
        border-radius: 5px;
        font-family: 'Courier New', monospace;
    }
    .grid-title {
        color: #00FF00;
        border-bottom: 1px solid #333;
        margin-bottom: 10px;
        padding-bottom: 5px;
        font-weight: bold;
    }
    .led-grid {
        display: grid;
        grid-template-columns: repeat(8, 1fr); /* แถวละ 8 ดวง */
        gap: 10px 5px;
        margin-bottom: 25px;
    }
    .led-item {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .led-circle {
        width: 18px;
        height: 18px;
        border-radius: 50%;
        border: 2px solid #333;
        transition: all 0.2s ease;
    }
    .on {
        background-color: #00FF00;
        box-shadow: 0 0 10px #00FF00;
        border-color: #fff;
    }
    .off {
        background-color: #002200; /* เขียวมืด */
        border-color: #222;
    }
    .led-label {
        color: #888;
        font-size: 10px;
        margin-top: 4px;
    }
</style>

<div class="plc-container">
    <div class="grid-title">INPUT STATUS (X)</div>
    <div class="led-grid">
        <div ng-repeat="item in msg.payload.inputs" class="led-item">
            <div class="led-circle" ng-class="item.val == 1 ? 'on' : 'off'"></div>
            <span class="led-label">{{item.lab}}</span>
        </div>
    </div>

    <div class="grid-title">OUTPUT STATUS (Y)</div>
    <div class="led-grid">
        <div ng-repeat="item in msg.payload.outputs" class="led-item">
            <div class="led-circle" ng-class="item.val == 1 ? 'on' : 'off'"></div>
            <span class="led-label">{{item.lab}}</span>
        </div>
    </div>
</div>